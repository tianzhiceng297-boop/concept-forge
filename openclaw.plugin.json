{
  "id": "concept-forge",
  "name": "Concept Forge",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "projectId": {
        "type": "string",
        "description": "Optional project identifier for ledger isolation. Defaults to 'default' if not set."
      },
      "autoIntervene": {
        "type": "boolean",
        "description": "Whether the plugin should automatically intervene in the conversation when signals are detected. Defaults to true.",
        "default": true
      },
      "synonymThreshold": {
        "type": "number",
        "description": "Jaro-Winkler similarity threshold for synonym detection. Defaults to 0.85.",
        "default": 0.85
      }
    }
  }
}